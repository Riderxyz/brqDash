import { FormControl } from '@angular/forms';

export interface ControlsInputRegister {
    dataNascimento: FormControl;
    email: FormControl;
    exteira: FormControl;
    nomeCompleto: FormControl;
    senha: FormControl;
}
